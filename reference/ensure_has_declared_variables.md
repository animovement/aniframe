# Ensure every declared variable names a column that exists

Ensure every declared variable names a column that exists

## Usage

``` r
ensure_has_declared_variables(data)
```

## Arguments

- data:

  An aniframe object.

## Value

`TRUE`, invisibly.
