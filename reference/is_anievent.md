# Check if object is an anievent

Check if object is an anievent

## Usage

``` r
is_anievent(x)
```

## Arguments

- x:

  An object to test.

## Value

Logical: `TRUE` if `x` inherits from `anievent`.
