# Coerce a metadata value to the factor its field expects

Coerce a metadata value to the factor its field expects

## Usage

``` r
as_metadata_factor(value, field)
```

## Arguments

- value:

  Character or factor value.

- field:

  Name of the metadata field.

## Value

A factor with the field's full set of levels.
