# The metadata fields that describe the frame's structure

Writing any of these has consequences beyond the metadata list, so they
are reachable only through their own setters.

## Usage

``` r
structural_metadata_fields()
```

## Value

Character vector of metadata field names.
