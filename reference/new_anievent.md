# Create a new anievent object (internal constructor)

Create a new anievent object (internal constructor)

## Usage

``` r
new_anievent(x)
```

## Arguments

- x:

  A data frame to convert to anievent.

## Value

An anievent object.
