# Test whether a data frame uses a spherical coordinate system

Requires `rho`, `phi` and `theta`; forbids `z`.

## Usage

``` r
is_spherical(data)
```

## Arguments

- data:

  A data frame.

## Value

Logical scalar.
