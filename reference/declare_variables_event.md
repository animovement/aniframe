# Declare the event columns and validate them against the frame

Declare the event columns and validate them against the frame

## Usage

``` r
declare_variables_event(data, state, point)
```

## Arguments

- data:

  An aniframe object.

- state, point:

  Character vectors of column names, or `NULL`.

## Value

`data`, with the declaration recorded.
