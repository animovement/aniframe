# Combine a partial axis map into the one already declared

Combine a partial axis map into the one already declared

## Usage

``` r
merge_axis_map(current, update)
```

## Arguments

- current, update:

  Named vectors of the same type.

## Value

`current` with `update` written over it, `NA` entries dropped.
