# Ensure every declared variable names a column that exists

Ensure every declared variable names a column that exists

## Usage

``` r
ensure_declared_variables_exist(data)
```

## Arguments

- data:

  An aniframe object.

## Value

`TRUE`, invisibly.
