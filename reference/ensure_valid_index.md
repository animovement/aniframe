# Ensure a proposed index is usable

Ensure a proposed index is usable

## Usage

``` r
ensure_valid_index(data, column)
```

## Arguments

- data:

  An aniframe object.

- column:

  The proposed index column.

## Value

`TRUE`, invisibly.
