# Test whether a data frame uses a cylindrical coordinate system

Requires `rho`, `phi` and `z`; forbids `theta`.

## Usage

``` r
is_cylindrical(data)
```

## Arguments

- data:

  A data frame.

## Value

Logical scalar.
