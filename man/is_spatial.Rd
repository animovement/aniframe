% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aniframe_validate.R
\name{is_spatial}
\alias{is_spatial}
\title{Test whether the spatial columns match the metadata}
\usage{
is_spatial(data)
}
\arguments{
\item{data}{An aniframe object.}
}
\value{
Logical scalar.
}
\description{
Returns \code{TRUE} when \code{variables_where} declares at least one column and
every column it names is present and numeric.
}
\details{
This is a different question from the one \code{\link[=is_cartesian]{is_cartesian()}} and its
siblings answer: those test for the presence of particular column
\emph{names} (\code{x}, \code{y}, \code{z}, …) and never consult the metadata or the column
types. A frame that has lost its \code{x} column still satisfies
\code{is_cartesian_1d()} on the strength of \code{y} alone, while its
\code{variables_where} still promises both.
}
\examples{
af <- aniframe(time = 1:5, x = 1:5, y = 1:5)
is_spatial(af)

# Dropping a declared column breaks the correspondence
is_spatial(dplyr::select(af, -x))

}
\seealso{
\code{\link[=ensure_is_spatial]{ensure_is_spatial()}}, \code{\link[=validate_aniframe]{validate_aniframe()}}.
}
