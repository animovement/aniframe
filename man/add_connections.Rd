% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connections.R
\name{add_connections}
\alias{add_connections}
\title{Add connections to an aniframe}
\usage{
add_connections(data, from, to, variable = "keypoint")
}
\arguments{
\item{data}{An aniframe object.}

\item{from}{Character vector of source endpoints.}

\item{to}{Character vector of target endpoints. Must be the same length
as \code{from}.}

\item{variable}{Character scalar. Name of the variable the connections
relate to (must be in \code{variables_what} or \code{variables_when}). Defaults
to \code{"keypoint"}.}
}
\value{
The aniframe with the new connections appended.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Append one or more \code{from}/\code{to} pairs to the existing connections for a
variable. \code{from} and \code{to} may be either single strings or character vectors
of equal length (one connection per element).
}
\details{
No deduplication is performed — duplicates of an existing pair will appear
twice in the resulting table. Endpoints not found in \code{data[[variable]]}
produce a warning but are still appended.
}
\examples{
\dontrun{
data <- example_aniframe()
data <- add_connections(data, from = "head", to = "neck")
data <- add_connections(
  data,
  from = c("neck", "neck"),
  to = c("shoulder_right", "shoulder_left")
)
}

}
\seealso{
\code{\link[=set_connections]{set_connections()}}, \code{\link[=get_connections]{get_connections()}}, \code{\link[=remove_connections]{remove_connections()}}
}
