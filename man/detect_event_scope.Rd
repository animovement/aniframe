% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anievent_as.R
\name{detect_event_scope}
\alias{detect_event_scope}
\title{Detect the identity / grouping scope of one event column}
\usage{
detect_event_scope(data, event_col, candidate_cols)
}
\description{
Returns the minimal subset of \code{candidate_cols} that the value of
\code{event_col} varies across (given \code{time}). A column is "redundant"
for an event channel when, after removing it from the candidate
set, \code{(remaining + time)} still uniquely determines the event value
on every non-\code{NA} row.
}
\details{
Used by \code{as_anievent.aniframe()} so that, e.g., a \code{behaviour} column
that is constant across keypoints for each \verb{(individual, time)}
drops \code{keypoint} from the resulting \code{anievent}'s grouping — instead
of emitting one identical bout per keypoint.

The detection is greedy: at each iteration, drop any column whose
removal still leaves the event value uniquely determined; stop when
no further removal is possible.
}
\keyword{internal}
