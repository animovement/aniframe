% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_events.R
\name{add_events}
\alias{add_events}
\title{Add events from an anievent onto an aniframe host}
\usage{
add_events(data, events)
}
\arguments{
\item{data}{An aniframe — the host whose time grid receives the events.}

\item{events}{An anievent.}
}
\value{
The host aniframe with one new column per \verb{(channel, type)} group on the events. The new columns are
factor-valued and \code{NA} outside any bout. Each column is
registered in \code{metadata$variables_event$state} or \verb{$point}
according to the row's \code{type}.
}
\description{
Joins an \code{\link[=anievent]{anievent()}} onto the per-frame time grid of an \code{\link[=aniframe]{aniframe()}}
host, adding one factor column per \verb{(channel, type)} group in
the anievent. For each row of the host, the new column carries the
\code{label} of the active bout on that group for the matching subject
(and observation / session / trial, if those are present on both
sides); \code{NA} where no bout is active.
}
\details{
Column naming uses suffix conventions on collisions:
\itemize{
\item If a channel carries only one \code{type}, the column is
\verb{<channel>} (no suffix).
\item If a channel carries both state and point, the columns are
\verb{<channel>_state} and \verb{<channel>_point}.
\item If bouts within a \verb{(channel, type)} group overlap, greedy
interval colouring splits them into tracks: track 1 keeps the
base name, tracks 2.. add \verb{_2}, \verb{_3}, … (so a state channel with
overlap might become \verb{<channel>_state} plus \verb{<channel>_state_2}).
}

Time-unit handling:
\itemize{
\item If both objects share \code{unit_time}, the join uses the values as-is.
\item If they differ but both are SI (\code{ms}, \code{s}, \code{m}, \code{h}), \code{events} is
converted to the host's unit.
\item If one side is in \code{"frame"} or \code{"unknown"}, a \code{sampling_rate} on
either side is used to bridge to seconds; without one the function
errors.
}

Identity / grouping columns: the join uses the intersection of
\code{(variables_what)} and the grouping part of \code{variables_when}
(everything except \code{time} on host, \code{start}/\code{stop} on events) between
the two objects. Frames outside any bout get \code{NA}; new column names
colliding with existing host columns error out.
}
\examples{
\dontrun{
af <- aniframe(individual = 1L, time = 1:10, x = 1:10, y = 1:10)
ae <- anievent(
  individual = 1L,
  channel = c("behaviour", "behaviour"),
  label = c("REM", "wake"),
  start = c(1, 5),
  stop = c(4, 10)
)
add_events(af, ae)
}

}
