% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connections.R
\name{remove_connections}
\alias{remove_connections}
\title{Remove connections from an aniframe}
\usage{
remove_connections(data, from, to, variable = "keypoint")
}
\arguments{
\item{data}{An aniframe object.}

\item{from}{Character vector of source endpoints to remove.}

\item{to}{Character vector of target endpoints to remove. Must be the same
length as \code{from}.}

\item{variable}{Character scalar. Name of the variable. Defaults to
\code{"keypoint"}.}
}
\value{
The aniframe with matching connections removed.
}
\description{
\strong{[Experimental]}

Remove \code{from}/\code{to} pairs from the connections of a variable. Matching is
exact and order-sensitive: \code{remove_connections(data, "a", "b")} removes
the pair \verb{(from = "a", to = "b")} but does \emph{not} remove \verb{(from = "b", to = "a")}. Call twice with swapped arguments if you want both directions
gone.
}
\examples{
\dontrun{
data <- example_aniframe() |>
  add_connections(from = c("head", "neck"), to = c("neck", "shoulder_right"))
data <- remove_connections(data, from = "head", to = "neck")
}

}
\seealso{
\code{\link[=set_connections]{set_connections()}}, \code{\link[=get_connections]{get_connections()}}, \code{\link[=add_connections]{add_connections()}}
}
