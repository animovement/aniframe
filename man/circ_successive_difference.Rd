% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular.R
\name{circ_successive_difference}
\alias{circ_successive_difference}
\title{Differences between successive angles in a series}
\usage{
circ_successive_difference(x, lag = 1L)
}
\arguments{
\item{x}{A numeric vector of angles, in radians.}

\item{lag}{A positive integer (default \code{1L}) giving the lag to difference at.}
}
\value{
A numeric vector the same length as \code{x}, in radians. The first \code{lag}
entries are \code{NA}; the rest are angular differences in \verb{(-pi, pi]}.
}
\description{
Applies \code{\link[=circ_difference]{circ_difference()}} along a vector, comparing each angle with the one
\code{lag} positions before it — the turn from one heading to the next, rather
than the difference between two angles you name. Unlike \code{\link[base:diff]{base::diff()}} the
result is the same length as \code{x}, padded with \code{NA} at the start, so it can be
used inside \code{\link[dplyr:mutate]{dplyr::mutate()}}.
}
\examples{
circ_successive_difference(c(0, pi / 2, pi, 3 * pi / 2))

# crossing zero is a small step, not a large one
circ_successive_difference(c(6.2, 0.1))

}
\seealso{
Other circular statistics:
\code{\link[=circ_difference]{circ_difference()}},
\code{\link[=circ_mad]{circ_mad()}},
\code{\link[=circ_mean]{circ_mean()}},
\code{\link[=circ_median]{circ_median()}},
\code{\link[=circ_sd]{circ_sd()}}
}
\concept{circular statistics}
