% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aniframe-package.R.R, R/aniframe.R
\docType{package}
\name{aniframe}
\alias{aniframe-package}
\alias{aniframe}
\title{aniframe package}
\usage{
aniframe(
  ...,
  metadata = list(),
  variables_what = NULL,
  variables_when = NULL,
  variables_where = NULL,
  .rows = NULL,
  .name_repair = c("check_unique", "unique", "universal", "minimal")
)
}
\arguments{
\item{...}{Name-value pairs to create columns in the data frame.}

\item{metadata}{Optional list of metadata.}

\item{variables_what}{Character vector of identity columns that together
define a unique entity, and which the frame is grouped by. If \code{NULL}
(the default), detected from the data: whichever of \code{model},
\code{individual}, \code{track} and \code{keypoint} are present. An aniframe needs
at least one identity variable, so if none of them is found, a
\code{keypoint} column is added with the value \code{"centroid"}. Pass
\code{character(0)} to declare no identity variables at all — a
deliberate opt-out, which leaves the frame ungrouped. Every column
named here must exist in \code{data}.}

\item{variables_when}{Character vector of temporal columns that together
define a unique timepoint. If \code{NULL} (the default), detected from the
data: whichever of \code{observation}, \code{session}, \code{trial} and \code{time} are
present. \code{time} is always required.}

\item{variables_where}{Character vector of spatial columns that together
define position. If \code{NULL} (the default), detected from the data.}

\item{.rows}{Number of rows (passed to tibble).}

\item{.name_repair}{How to repair column names (passed to tibble).}
}
\value{
An aniframe object (tibble with aniframe class).
}
\description{
An R package providing core data structures for movement data.

Creates a specialized data frame for movement data with columns defining
entity identity, timepoints, and spatial position.
}
\examples{
aniframe(
  individual = rep(1:2, each = 25),
  time = rep(1:10, 5),
  x = rnorm(50),
  y = rnorm(50)
)

# Custom variables
aniframe(
  track = rep(1:3, each = 10),
  trial = 1,
  time = rep(1:10, 3),
  x = rnorm(30),
  y = rnorm(30),
  variables_what = "track",
  variables_when = c("trial", "time")
)
}
\seealso{
Useful links:
\itemize{
  \item \url{http://animovement.dev/aniframe/}
  \item \url{https://github.com/animovement/aniframe/}
  \item Report bugs at \url{https://github.com/animovement/aniframe/issues}
}

}
\author{
\strong{Maintainer}: Mikkel Roald-Arbøl \email{animovement.84w1m@passmail.com} (\href{https://orcid.org/0000-0002-9998-0058}{ORCID})

Authors:
\itemize{
  \item Mikkel Roald-Arbøl \email{animovement.84w1m@passmail.com} (\href{https://orcid.org/0000-0002-9998-0058}{ORCID})
}

}
\keyword{internal}
