% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{convert_inf_to_na}
\alias{convert_inf_to_na}
\title{Convert Inf to NA in numeric columns}
\usage{
convert_inf_to_na(data)
}
\arguments{
\item{data}{A data frame.}
}
\value{
A data frame with \code{Inf} and \code{-Inf} replaced by \code{NA} in numeric
columns.
}
\description{
Replaces all \code{Inf} and \code{-Inf} values with \code{NA} in numeric columns of a
data frame. The sibling of \code{\link[=convert_nan_to_na]{convert_nan_to_na()}}, for sources that mark a
missing observation with an infinity rather than a \code{NaN} — TRex is one,
and its own documentation masks \code{np.inf} out before plotting.
}
\details{
Worth doing at read time rather than later: an \code{Inf} propagates through
arithmetic silently, so a single untracked frame turns a mean, a speed or
a bounding box into \code{Inf} rather than into a missing value.
}
\examples{
df <- data.frame(x = c(1, Inf, -Inf, 3))
convert_inf_to_na(df)
}
