% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anievent_as.R
\name{as_anievent}
\alias{as_anievent}
\alias{as_anievent.anievent}
\alias{as_anievent.data.frame}
\title{Convert a data frame to an anievent}
\usage{
as_anievent(
  data,
  metadata = list(),
  variables_what = NULL,
  variables_when = NULL
)

\method{as_anievent}{anievent}(
  data,
  metadata = list(),
  variables_what = NULL,
  variables_when = NULL
)

\method{as_anievent}{data.frame}(
  data,
  metadata = list(),
  variables_what = NULL,
  variables_when = NULL
)
}
\arguments{
\item{data}{A data frame with the required columns.}

\item{metadata}{Optional list of metadata.}

\item{variables_what}{Character vector of identity columns. When
\code{NULL} (default), auto-detected from a known list (\code{model},
\code{individual}, \code{track}, \code{subject}) — only those present in \code{data} are
used. Pass an explicit value to use any other column name(s) as
identity. An anievent with no identity column is permitted (e.g. a
single-subject experiment).}

\item{variables_when}{Character vector of temporal columns. When
\code{NULL} (default), auto-detected from a known grouping list
(\code{observation}, \code{session}, \code{trial}) and concatenated with the
required temporal endpoints \code{c("start", "stop")}. Pass explicitly
to use other names for the grouping context.}
}
\value{
An anievent object.
}
\description{
Builds an \code{anievent} from a data frame holding behavioural events in
long format (one row per bout or instant). The five mandatory columns
are \code{channel}, \code{type}, \code{label}, \code{start}, and \code{stop};
identity columns travel via \code{variables_what}. An optional \code{modifiers}
list-column may carry per-event modifier values (each cell a character
vector, matching the BORIS export format).
}
