% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables_when_index.R
\name{set_index}
\alias{set_index}
\title{Declare which column an aniframe is indexed by}
\usage{
set_index(data, column)
}
\arguments{
\item{data}{An aniframe object.}

\item{column}{Length-one character vector naming the index column. It
must exist in \code{data} and be numeric.}
}
\value{
\code{data}, re-indexed and restructured.
}
\description{
Changing the index changes what the frame is grouped by and the order
its rows come in, so — like the \verb{variables_*} declarations — it is not
reachable through \code{\link[=set_metadata]{set_metadata()}} and has its own setter, which does
the restructuring too.
}
\details{
The column is added to \code{variables_when} if it is not already there.
}
\examples{
df <- data.frame(frame = 1:3, individual = "a", x = c(1, 2, 3), y = c(0, 1, 0))
af <- as_aniframe(df, index = "frame")
get_index(af)

}
\seealso{
\code{\link[=get_index]{get_index()}}
}
