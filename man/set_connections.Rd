% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connections.R
\name{set_connections}
\alias{set_connections}
\title{Set the connections for a variable}
\usage{
set_connections(data, connections, variable = "keypoint")
}
\arguments{
\item{data}{An aniframe object.}

\item{connections}{One of:
\itemize{
\item a 2-column data.frame with columns \code{from} and \code{to},
\item a list of length-2 character vectors (each \code{c(from, to)}),
\item \code{NULL} to clear the connections for \code{variable}.
}}

\item{variable}{Character scalar. Name of the identity (\code{variables_what})
or temporal (\code{variables_when}) variable the connections relate to.
Defaults to \code{"keypoint"}.}
}
\value{
The aniframe with updated \code{connections} metadata.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Replace the connections (e.g. skeleton edges between keypoints, edges of a
social network between individuals) for a single variable. Connections are
stored as a 2-column \code{from}/\code{to} tibble; storage preserves the order
supplied so downstream consumers can interpret the table as either
directed or undirected.
}
\details{
If any \code{from}/\code{to} value isn't found in the corresponding column of \code{data},
a warning is emitted but the connection is kept — the value may legitimately
be missing in this particular recording while being valid elsewhere.
}
\examples{
\dontrun{
data <- example_aniframe()

# Implicit by position (element[1] = from, element[2] = to)
data <- set_connections(
  data,
  list(
    c("head", "neck"),
    c("neck", "shoulder_right"),
    c("neck", "shoulder_left"),
    c("shoulder_right", "hip_right"),
    c("shoulder_left", "hip_left")
  )
)

# Explicit names within each pair
data <- set_connections(
  data,
  list(
    c(from = "head", to = "neck"),
    c(from = "neck", to = "shoulder_right")
  )
)

# Or as a 2-column data.frame
data <- set_connections(
  data,
  data.frame(
    from = c("head", "neck"),
    to   = c("neck", "shoulder_right")
  )
)
}

}
\seealso{
\code{\link[=get_connections]{get_connections()}}, \code{\link[=add_connections]{add_connections()}}, \code{\link[=remove_connections]{remove_connections()}}
}
