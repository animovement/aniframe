% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{recognised_variables_what}
\alias{recognised_variables_what}
\title{Identity variable names recognised across the animovement classes}
\usage{
recognised_variables_what()
}
\value{
Character vector of column names.
}
\description{
The identity (\code{what}) columns auto-detection looks for, shared by
\code{\link[=as_aniframe]{as_aniframe()}} and \code{\link[=as_anievent]{as_anievent()}}. The order is coarse to fine — a
\code{subject} or \code{individual} has \code{track}s, a track has \code{keypoint}s — and
it carries through to column order and grouping. Only the names
present in the data are used, and any other column can be declared
explicitly via \code{variables_what}.
}
\details{
\code{subject} and \code{individual} name the same kind of thing; both are
recognised because behavioural coding tools (BORIS and its kin) speak
of subjects where tracking tools speak of individuals.
}
\keyword{internal}
