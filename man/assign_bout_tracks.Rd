% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_events.R
\name{assign_bout_tracks}
\alias{assign_bout_tracks}
\title{Greedy interval-colour bouts per identity-group into tracks}
\usage{
assign_bout_tracks(bouts, ev_key)
}
\arguments{
\item{bouts}{A data frame of bouts (one channel's worth).}

\item{ev_key}{A character vector the same length as \code{nrow(bouts)}
identifying which identity-group each bout belongs to.}
}
\value{
Integer vector of track assignments (same length as
\code{nrow(bouts)}).
}
\description{
Bouts that overlap (or touch at a single point — \code{start == stop} of
previous bout) are assigned to different tracks within their
identity-group, so each track is mutually exclusive. Bouts in
different groups can share a track (different rows on the
aniframe). Track 1 is preferred where possible.
}
\keyword{internal}
