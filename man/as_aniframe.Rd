% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aniframe_as.R
\name{as_aniframe}
\alias{as_aniframe}
\title{Convert a data frame to aniframe}
\usage{
as_aniframe(
  data,
  metadata = list(),
  variables_what = NULL,
  variables_when = NULL,
  variables_where = NULL,
  index = NULL
)
}
\arguments{
\item{data}{A data frame with movement data.}

\item{metadata}{A list of metadata to attach to the aniframe.}

\item{variables_what}{Character vector of identity columns that together
define a unique entity, and which the frame is grouped by. If \code{NULL}
(the default), detected from the data: whichever of \code{model},
\code{individual}, \code{subject}, \code{track} and \code{keypoint} are present, in that
order (coarse to fine). An aniframe needs
at least one identity variable, so if none of them is found, a
\code{keypoint} column is added with the value \code{"centroid"}. Pass
\code{character(0)} to declare no identity variables at all — a
deliberate opt-out, which leaves the frame ungrouped. Every column
named here must exist in \code{data}.}

\item{variables_when}{Character vector of temporal columns that together
define a unique timepoint. If \code{NULL} (the default), detected from the
data: whichever of \code{observation}, \code{session}, \code{trial} and \code{time} are
present, minus the index. These are the temporal \emph{context} — which
session, which trial — and, together with \code{variables_what}, they are
what the frame is grouped by. The index itself is declared separately
and is never one of them.}

\item{variables_where}{Character vector of spatial columns that together
define position. If \code{NULL} (the default), detected from the data.}

\item{index}{Length-one character vector naming the column the frame is
indexed by — the position of each row within its temporal context.
It is never a grouping variable. If
\code{NULL} (the default), the frame's existing declaration is kept, or
\code{"time"} for a frame that has none. The column must exist and be
numeric; it may be called anything.}
}
\value{
An aniframe object
}
\description{
Convert a data frame to aniframe
}
\examples{
df <- data.frame(
  time = 1:3, individual = 'a', keypoint = 'centroid',
  x = c(0, 1, 2), y = c(0, 1, 0)
)
as_aniframe(df)
}
