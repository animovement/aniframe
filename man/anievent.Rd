% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anievent.R
\name{anievent}
\alias{anievent}
\title{Create an anievent data frame}
\usage{
anievent(
  ...,
  metadata = list(),
  variables_what = NULL,
  variables_when = NULL,
  .rows = NULL,
  .name_repair = c("check_unique", "unique", "universal", "minimal")
)
}
\arguments{
\item{...}{Name-value pairs to create columns in the data frame.}

\item{metadata}{Optional list of metadata.}

\item{variables_what}{Character vector of identity columns that together
define a unique entity. When \code{NULL} (default), auto-detected from a
known list (\code{model}, \code{individual}, \code{track}, \code{subject}).}

\item{variables_when}{Character vector of temporal columns. When
\code{NULL} (default), auto-detected from a known grouping list
(\code{observation}, \code{session}, \code{trial}) and concatenated with the
required temporal endpoints \code{c("start", "stop")}.}

\item{.rows}{Number of rows (passed to tibble).}

\item{.name_repair}{How to repair column names (passed to tibble).}
}
\value{
An anievent object.
}
\description{
Creates a specialised data frame for behavioural events in long format:
one row per bout (state event) or instant (point event). The class is a
sibling of \code{\link[=aniframe]{aniframe()}} — it shares the metadata substrate but holds
event-bout records rather than per-frame movement data.
}
\details{
Mandatory columns: \code{variable}, \code{value}, \code{start}, \code{stop}. Identity
columns (e.g. \code{individual}, \code{subject}, \code{track}) are optional and
declared via \code{variables_what}. A \code{modifiers} list-column may carry
per-event modifier values — each cell a character vector (matching
the BORIS export format, where one event can have zero or more
modifier values selected from the ethogram).
}
\examples{
anievent(
  individual = c(1L, 1L, 1L),
  variable = c("behaviour", "behaviour", "call"),
  value = c("REM", "wake", "alarm"),
  start = c(3, 14, 4.5),
  stop = c(9, 19, 4.5)
)
}
