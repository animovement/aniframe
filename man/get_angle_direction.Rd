% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orientation.R
\name{get_angle_direction}
\alias{get_angle_direction}
\title{Which way angles run}
\usage{
get_angle_direction(data)
}
\arguments{
\item{data}{An aniframe or anievent object.}
}
\value{
\code{"clockwise"}, \code{"counter_clockwise"}, or \code{"unknown"} when the two
axes are not both declared or do not span the view.
}
\description{
The sense of rotation from the \code{x} axis to the \code{y} axis, as seen from
where the recording was made. \code{atan2(y, x)} counts counter-clockwise, so
a frame stored the other way up reports the mirror of the angle a
\code{counter_clockwise} frame would give for the same physical heading.

Derived from \code{\link[=get_axis_directions]{get_axis_directions()}} rather than recorded, so it cannot
go on claiming a sense the axes no longer have.
}
\examples{
af <- example_aniframe(n_obs = 3, n_individuals = 1, n_keypoints = 1)

# An image-plane frame counts angles clockwise
af <- set_axis_directions(af, c(x = "right", y = "down"))
get_angle_direction(af)

}
\seealso{
\code{\link[=get_axis_directions]{get_axis_directions()}}, \code{\link[=get_handedness]{get_handedness()}}
}
