% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anievent_to.R
\name{to_anievent}
\alias{to_anievent}
\alias{to_anievent.anievent}
\alias{to_anievent.data.frame}
\alias{to_anievent.aniframe}
\title{Encode per-frame data into an anievent}
\usage{
to_anievent(data, ...)

\method{to_anievent}{anievent}(data, ...)

\method{to_anievent}{data.frame}(
  data,
  time,
  state = NULL,
  point = NULL,
  variables_what = NULL,
  variables_when = NULL,
  metadata = list(),
  ...
)

\method{to_anievent}{aniframe}(
  data,
  variables_what = NULL,
  variables_when = NULL,
  metadata = list(),
  ...
)
}
\arguments{
\item{data}{A data frame or an \code{\link[=aniframe]{aniframe()}}.}

\item{...}{Passed to methods.}

\item{time}{For data-frame input, the column holding per-frame
times. Bare name (tidyselect). Required.}

\item{state}{For data-frame input, columns to run-length-encode
as state bouts. Bare names (tidyselect). Logical columns produce
bouts on TRUE-runs, labelled by the column name; factor or
character columns produce one bout per contiguous non-\code{NA} run
of the same value.}

\item{point}{For data-frame input, columns to encode as point
bouts. Bare names (tidyselect). Logical columns produce one
point bout per TRUE frame, labelled by the column name; factor
or character columns produce one bout per non-\code{NA} frame.}

\item{variables_what}{For data-frame input, identity columns
(e.g. \code{individual}). Bare names (tidyselect). Bouts are
isolated per identity group.}

\item{variables_when}{For data-frame input, additional temporal-
grouping columns (e.g. \code{observation}, \code{session}, \code{trial}). Bare
names (tidyselect). Like identity, these isolate bouts.}

\item{metadata}{Optional list of metadata attached to the result.
For an aniframe input, fields like \code{unit_time} and
\code{sampling_rate} are propagated automatically; \code{metadata}
overrides those.}
}
\value{
An \code{\link[=anievent]{anievent()}}.
}
\description{
Run-length-encodes per-frame state and point variables into the
long-format \code{\link[=anievent]{anievent()}}. Works on a data frame (with bare-name
selection of the event columns and explicit \code{time} / identity)
or on an \code{\link[=aniframe]{aniframe()}} (where everything is read from metadata).
}
\details{
Distinct from \code{\link[=as_anievent]{as_anievent()}}: that one is a strict cast — the
input must already be in canonical anievent shape (one row per
bout, with \code{channel} / \code{type} / \code{label} / \code{start} / \code{stop}).
\code{to_anievent()} is the encoding verb that \emph{produces} that shape
from per-frame data.
}
\examples{
\dontrun{
library(tibble)
df <- tibble(
  individual = 1L,
  time = 1:8,
  behaviour = factor(c("REM", "REM", "REM", "wake", "wake", "REM", "REM", NA)),
  woke_up = c(FALSE, FALSE, FALSE, TRUE, FALSE, FALSE, FALSE, FALSE),
  call = c(NA, "alarm", NA, NA, NA, NA, NA, NA)
)
to_anievent(
  df,
  time = time,
  state = c(behaviour, woke_up),
  point = call,
  variables_what = individual
)
}

}
