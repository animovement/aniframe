% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{is_sampling_regular}
\alias{is_sampling_regular}
\title{Is the frame regularly sampled?}
\usage{
is_sampling_regular(data, tolerance = 1e-06)
}
\arguments{
\item{data}{An aniframe object.}

\item{tolerance}{Relative tolerance: a gap counts as equal to the
interval when it differs by no more than \code{tolerance * interval}.
Timestamps are rarely exactly equal, so comparing them with \code{==} says
"irregular" for data that is regular to any precision that matters.
Raise it for noisy timestamps, lower it to be strict.}
}
\value{
\code{TRUE}, \code{FALSE}, or \code{NA} when the frame is too short to tell.
}
\description{
Every gap between consecutive observations equal, within \code{tolerance}.
Computed from the data each time it is asked rather than recorded,
because dropping rows changes the answer and a stored logical would go
on claiming the old one.
}
\examples{
af <- example_aniframe(n_obs = 5, n_individuals = 2, n_keypoints = 1)
is_sampling_regular(af)

# A gap in the recording
irregular <- af |> dplyr::filter(time != 3)
is_sampling_regular(irregular)

}
\seealso{
\code{\link[=get_sampling_interval]{get_sampling_interval()}}
}
