% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_origin.R
\name{set_origin}
\alias{set_origin}
\title{Set the coordinate origin}
\usage{
set_origin(data, origin)
}
\arguments{
\item{data}{An aniframe object.}

\item{origin}{Character. One of \code{"bottom_left"} or \code{"top_left"}.}
}
\value{
The aniframe with reflected y coordinates (when the origin changed)
and updated \code{origin} metadata.
}
\description{
Sets or updates the \code{origin} metadata field, which records where the (0,0)
coordinate sits relative to the recording frame. When the new origin
differs from the current one, the y coordinates are reflected around
\code{y_height} so the data is expressed in the new convention.
}
\details{
The flip uses the formula \code{y_new = y_height - y_old}. The \code{y_height}
metadata field must therefore be set when the origin actually changes; if
it is \code{NA}, this function errors and asks the user to set it via
\code{\link[=set_y_height]{set_y_height()}}. When the supplied \code{origin} matches the current value,
the data is returned unchanged.
}
\examples{
\dontrun{
data <- example_aniframe()
data <- set_y_height(data, y_height = 1080)
data <- set_origin(data, origin = "top_left")
}

}
\seealso{
\code{\link[=set_y_height]{set_y_height()}}
}
