% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aniframe_validate.R
\name{validate_aniframe}
\alias{validate_aniframe}
\title{Validate an aniframe}
\usage{
validate_aniframe(data)
}
\arguments{
\item{data}{An aniframe object.}
}
\value{
The input \code{data}, invisibly.
}
\description{
Re-checks, on demand, that an \code{aniframe}'s metadata still describes the
frame it is attached to. The two drift apart silently under ordinary
dplyr work: \code{\link[dplyr:select]{dplyr::select()}} drops a column without touching the
metadata that names it, and assignment can change a column's type. The
invariants are therefore checked rather than assumed:
}
\details{
\itemize{
\item the index column is present and numeric — hard error;
\item every column named in \code{variables_what}, \code{variables_when},
\code{variables_where} and \code{variables_event} is present in the data —
hard error;
\item every column named in \code{variables_where} is numeric — hard error;
\item \code{coordinate_system} agrees with \code{variables_where} — \strong{warning}
only. The frame is still usable, and the field is derived rather
than declared, so it can be refreshed;
\item identity, temporal context and the index together name one
observation per row — \strong{warning} only (#49);
\item a declared \code{sampling_rate} agrees with the spacing of the index —
\strong{warning} only (#114).
}
}
\examples{
af <- aniframe(time = 1:5, x = 1:5, y = 1:5)
validate_aniframe(af)

}
\seealso{
\code{\link[=ensure_is_spatial]{ensure_is_spatial()}} for the spatial subset of these checks,
which is the part downstream filters need on every call;
\code{\link[=validate_anievent]{validate_anievent()}} for the \code{anievent} equivalent.
}
