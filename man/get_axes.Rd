% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axes.R
\name{get_axes}
\alias{get_axes}
\title{The axis roles of an aniframe, and the columns carrying them}
\usage{
get_axes(data)
}
\arguments{
\item{data}{An aniframe object.}
}
\value{
Named character vector: names are axis roles (\code{x}, \code{y}, \code{z},
\code{rho}, \code{phi}, \code{theta}), values are the columns carrying them. Empty
for a frame whose coordinate system is \code{"unknown"}.
}
\description{
Where \code{\link[=get_variables_where]{get_variables_where()}} gives the column names, this gives what
each of them \emph{means}. Index by role to write a transformation that does
not care what the columns are called:
\code{data[[get_axes(data)[["x"]]]]}.
}
\examples{
af <- example_aniframe(n_obs = 3, n_individuals = 1, n_keypoints = 1)
get_axes(af)

df <- data.frame(time = 1:3, individual = "a", u = c(1, 2, 3), v = c(0, 1, 0))
renamed <- as_aniframe(df, variables_where = c(x = "u", y = "v"))
get_axes(renamed)
get_metadata(renamed, "coordinate_system")

}
\seealso{
\code{\link[=set_axes]{set_axes()}} to change it, \code{\link[=get_variables_where]{get_variables_where()}} for the
columns without their roles.
}
