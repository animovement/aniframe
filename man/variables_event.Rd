% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables_event.R
\name{variables_event}
\alias{variables_event}
\alias{get_variables_event}
\alias{set_variables_event}
\alias{add_variables_event}
\alias{remove_variables_event}
\title{Declare which columns carry per-frame event labels}
\usage{
get_variables_event(data)

set_variables_event(data, state = NULL, point = NULL)

add_variables_event(data, state = NULL, point = NULL)

remove_variables_event(data, variables)
}
\arguments{
\item{data}{An aniframe object.}

\item{state, point}{Character vectors of column names.}

\item{variables}{Character vector of column names to undeclare.}
}
\value{
For the setters, \code{data} with the declaration recorded. For
\code{get_variables_event()}, a named list with \code{state} and \code{point}
entries.
}
\description{
\code{variables_event} names the \code{aniframe} columns holding per-frame
categorical event labels, split into two kinds:
\itemize{
\item \strong{state} columns are interval-valued — a run of identical values is
one durative bout. List them coarse to fine when they nest.
\item \strong{point} columns are instantaneous — every non-\code{NA} frame is its
own zero-length event.
}

\code{\link[=to_anievent]{to_anievent()}} reads the declaration to know what to encode, and the
print header surfaces it as "State event variables" / "Point event
variables".

These functions declare the columns and check they exist, so the
metadata cannot promise a column the frame doesn't have.
\code{\link[=set_metadata]{set_metadata()}} refuses the field for that reason.
\itemize{
\item \code{set_variables_event()} replaces the declaration. A side you leave
as \code{NULL} is cleared, so \code{set_variables_event(data, state = "x")}
declares one state column and no point columns.
\item \code{add_variables_event()} appends to either side, leaving the other
untouched.
\item \code{remove_variables_event()} drops the named columns from whichever
side they are on — a column can only be one kind, so it needs no
\code{state} / \code{point} argument.
\item \code{get_variables_event()} reads the declaration back as a named list.
}

Only an \code{aniframe} can carry this declaration: an \code{anievent} is
already the encoded form, with its events in \code{channel} and \code{label}.
}
\examples{
af <- aniframe(
  time = 1:5,
  x = 1:5,
  y = 1:5,
  behaviour = factor(c("REM", "REM", "wake", "wake", "wake")),
  call = factor(c(NA, "alarm", NA, NA, NA), levels = "alarm")
)

af <- set_variables_event(af, state = "behaviour", point = "call")
get_variables_event(af)

# Declaring a column that isn't there is caught
try(add_variables_event(af, state = "grooming"))

}
\seealso{
\code{\link[=to_anievent]{to_anievent()}}, which consumes the declaration;
\code{\link[=set_variables_what]{set_variables_what()}} and friends for the other variable roles.
}
