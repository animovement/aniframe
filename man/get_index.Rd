% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables_when_index.R
\name{get_index}
\alias{get_index}
\title{The column an aniframe is indexed by}
\usage{
get_index(data)
}
\arguments{
\item{data}{An aniframe or anievent object.}
}
\value{
Length-one character vector naming the index column.
}
\description{
Exactly one column, of any name, holding the position of each row
within its trajectory. It is always one of the \code{variables_when}; the
rest of that vector is the surrounding temporal context — session,
trial, observation — and is what the frame is grouped by.
}
\examples{
af <- example_aniframe(n_obs = 3, n_individuals = 1, n_keypoints = 1)
get_index(af)

}
\seealso{
\code{\link[=set_index]{set_index()}} to change it, \code{\link[=get_variables_when]{get_variables_when()}} for the
full set of temporal columns.
}
