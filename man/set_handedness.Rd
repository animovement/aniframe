% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orientation.R
\name{set_handedness}
\alias{set_handedness}
\title{Say whether the frame is right- or left-handed}
\usage{
set_handedness(data, handedness = "right")
}
\arguments{
\item{data}{An aniframe object.}

\item{handedness}{Either \code{"right"} or \code{"left"}. Right-handed is the
convention across the suite and the default here; a frame is only
left-handed if it is told to be.}
}
\value{
The aniframe, with the axis directions that give this handedness
and the depth axis reflected if it had to turn over.
}
\description{
Handedness is what 3D data is usually described by, so this says it
directly rather than through three separate axis directions.

With two axes declared the third follows, and is recorded. With all three
declared, turning the handedness over reverses the \strong{depth} axis — the one
pointing \code{back} or \code{forward} — and reflects that column, which is the
conventional way to convert between the two.

Right-handed is the convention across the suite, so \code{set_handedness(data)}
completes a frame the standard way. It is not assumed of a frame that has
not been asked: which side a recording was made from is a fact about the
recording, and \code{\link[=get_handedness]{get_handedness()}} reports \code{"unknown"} until it is told.
}
\details{
Two axes cannot fix a handedness, so at least two must already be declared
for the third to follow from this one. Declare them with
\code{\link[=set_axis_directions]{set_axis_directions()}}.
}
\examples{
af <- example_aniframe(n_obs = 3, n_individuals = 1, n_keypoints = 1)
af <- set_axis_directions(af, c(x = "right", y = "up"))

# z follows from the handedness, which defaults to right
af <- set_handedness(af)
get_axis_directions(af)

}
\seealso{
\code{\link[=get_handedness]{get_handedness()}}, \code{\link[=set_axis_directions]{set_axis_directions()}}
}
